# Core simulation and analysis stack.
# Tested with Python 3.10-3.12 on macOS (Apple Silicon and Intel) and Linux.
mujoco>=3.1
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
PyYAML>=6.0
Pillow>=10.0

# Video recording (sim.record_video). imageio-ffmpeg ships its own ffmpeg binary.
imageio>=2.31
imageio-ffmpeg>=0.4.9

# Test runner (optional but recommended; the suite also has a built-in fallback).
pytest>=7.4
